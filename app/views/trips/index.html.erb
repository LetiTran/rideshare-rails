<h1>Rideshare Trips</h1>

<ul>
  <li>TRIP ID</li>
  <li>DRIVER ID</li>
  <li>PASSANGER ID</li>
</ul>



<% @trips.each do |trip| %>
  <% passenger = @passengers.find(trip.passenger_id) %>


  <ul >
    <li>  <%= link_to trip.id, show_trip_path(trip[:id])%>
      <% if trip.driver_exist %>
        <% driver = @drivers.find(trip.driver_id) %>
        <%= link_to driver.id, driver_path(driver[:id])%>
      <% else %>
        <%= trip[:driver_id] %>
      <% end %>

      <%= link_to passenger.id , show_passenger_path(passenger[:id])%> </li>
  </ul>

<% end %>
